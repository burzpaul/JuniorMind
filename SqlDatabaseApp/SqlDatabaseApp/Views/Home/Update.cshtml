@model SqlDatabaseApp.Artist

<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>vire2</title>
</head>
<body>
    <form asp-action="update" class="updatArtist" method="put">
        <label>Artist Id: @Model.ArtistId </label>
        <div class="updatArtist_Name">
            <label>Artist Name: </label>
            <input asp-for="Name" />
            @Html.HiddenFor(ArtistId => Model.ArtistId)
            <span asp></span>
        </div>
        <input class="updatArtist_submit" type="a" value="Update" />
    </form>
</body>
</html>